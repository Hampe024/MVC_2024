{% set sortedHighscores = highscore | sort((a, b) => a.score <=> b.score) | reverse %}

<table>
    <thead>
        <tr>
            <th>#</th>
            <th>Namn</th>
            <th>Poäng</th>
        </tr>
    </thead>
    <tbody>
        {% for index in 1..9 %}
            <tr>
                <td>{{ index }}</td>
                <td>{{ sortedHighscores[index - 1].name ?? "" }}</td>
                <td>{{ sortedHighscores[index - 1].score ?? 0 }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>