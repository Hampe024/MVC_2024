{% extends "base.html.twig" %}

{% block title %}Poker Squares{% endblock %}

{% block body %}
    <div class="poker-squares">
        {% if pokerSquares is defined %}
            <div class="poker-grid">
                {% for row in pokerSquares.getBoard().print() %}
                    {% for cell in row %}
                        <div class="poker-card">
                            {{ cell is null ? "🂠" : cell }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}